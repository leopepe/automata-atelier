  # Insert this attribute into `maintainers/maintainer-list.nix` in
  # alphabetical order (between `leona` and `leothrix` in the current tree,
  # adjust if those have moved). nixpkgs validates this file with a sort
  # check on every PR — `meta-check` will complain if the entry lands out
  # of order.
  leopepe = {
    # Email is optional; leave it commented if you want to keep yours
    # private. If you do provide one, prefer a project-specific address —
    # the file is publicly indexed.
    # email = "you@example.com";
    github = "leopepe";
    githubId = 446756;
    name = "Leonardo Pepe";
  };
